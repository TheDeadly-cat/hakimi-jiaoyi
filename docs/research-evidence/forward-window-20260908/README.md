# 72小时前向观察窗口结果

固定窗口已于 **2026-09-08 15:00 UTC / 北京时间23:00** 结束。报告时点为 **15:05:31.777052 UTC**。72个计划小时、两策略共144个策略小时均已到期：**106准时、14迟到、24缺失、0有实际证据的失败**。

本次完成的是固定窗口的运行记录汇总。存在迟到和缺失，尚不能证明持续准时运行；规范状态保留为 `WINDOW_ELAPSED_REVIEW_REQUIRED`。原小时观察继续运行，未合并PR、发布软件或开启账户/订单功能。

## 覆盖与时效

| 状态 | 策略小时 | 占144个计划策略小时 |
|---|---:|---:|
| ON_TIME | 106 | 73.6111% |
| LATE | 14 | 9.7222% |
| MISSING | 24 | 16.6667% |
| FAILED | 0 | 0% |
| 已取得观察（ON_TIME + LATE） | 120 | 83.3333% |

Dual MA与RSI各为53准时、7迟到、12缺失。没有未来或待到期小时。

缺失为以下12个小时的双策略记录（UTC）：9月5日21:00–23:00；9月6日00:00–05:00及08:00；9月7日06:00–07:00。迟到为9月5日15:00、16:00、20:00，9月6日09:00、14:00，9月7日08:00，9月8日12:00。缺失原因不能仅凭文件不存在推断，缺口未回填。

每个小时保留原始时间和唯一状态。早期报告已记为MISSING的分类保持冻结；后来出现的合法迟到观察也不能改写该分类，物理文件数量与冻结覆盖比例口径不同。本窗口最终缺失行没有后来取得的观察。

窗口选定范围为9月5日15:00 UTC（含）至9月8日15:00 UTC（不含），最后一个cutoff为9月8日14:00。计划在9月6日选定，早期结果已有部分被查看；它是工程观察窗口，不是预注册盈利样本。

## 实际运行证据

窗口内保留53次包装工具调用的完整开始/结束回执：52次SCHEDULED调用、一次MANUAL重复验证；53次均以退出码0结束，原有文件字节保持不变。没有失败重试、未结束回执或恢复事件。这不代表部署包装工具之前不存在重试或未被记录的调度事件。

新工具的第一份自动回执记录实际触发 **9月6日09:10:20.391 UTC**，实际开始09:11:24.244844、结束09:11:28.111804；其09:00双策略观察保留为LATE。更早的原驱动自动周期证据仍保留在覆盖报告内，不把两种首次运行混为一谈。

53次窗口内回执记录49次静默、4次通知决定；这些是程序的通知决定，不是终端用户实际收到通知的证明。真实触发、命令开始/结束和退出码可以在 `attempts/` 对应目录检查；过去未记录的进程时钟仍为null。

本次报告重新核验并重放了本机现有124条观察：120条属于窗口、2条为窗口前首次人工观察、2条为窗口后9月8日15:00观察。后两组均不进入144策略小时分母。本次公开的 `records/` 包含窗口内120条和原首次人工2条，共122份原字节文件；窗口后的观察继续留在部署目录。

## 可复查文件与边界

- [规范覆盖报告](coverage_1d07fb9ad16fafde7f64cd504ffe46bd596fecb84ec7e558061729bfe7c3574e.json)：144行、实际时钟、输入/记录身份、缺口、原自动周期投影和源环境核验结果。
- `records/`：122份原始观察JSON，文件名及SHA256对应报告中的 `record_file` 和 `record_file_sha256`。
- `attempts/`：53次窗口内调用的106份原始 `started.json` / `ended.json`，保留各自规范摘要及开始/结束关联。
- [冻结计划](../../studies/forward-reliability-plan-20260906.json)及[统计与运行契约](../../forward-reliability.md)。

报告规范摘要为 `1d07fb9ad16fafde7f64cd504ffe46bd596fecb84ec7e558061729bfe7c3574e`，文件SHA256为 `f2738d824582a90fae3efb348117f37ef5ec48d7fbd8e25fdf185131ebb09083`。本目录229份JSON复制时核验原字节、规范摘要、矩阵和回执关联，未发现绝对机器路径。行情原始响应、完整输入快照及命令stdout/stderr仍在本机，未重复发布；仅靠本目录不能独立重放全部行情输入。

部署保持0.2.1，运行源码摘要 `48f1c48875b774ccf0af732c0b7089a5b7ff1ae3bac1dd68ee357fc40ef6ceb5` 为BUILD_VERIFIED，依赖环境 `eb9a19e1db1204b430c9f35f380d107f858fc3263703eabc2e1bd64e315d376e` 为VERIFIED。部署包、冻结工具和计划未升级或改写。

全部观察仍为 `FLAT_REFERENCE_OBSERVATION`，没有持续参考持仓或前向PnL。ON_TIME仅指300秒标准；新信号的参考成交资格时间仍是实际可用后的首个小时开盘，未生成成交价或订单。

下一步是依据实际缺口和迟到评估调度改进；如以后改变调度方式或验收标准，应另行明确后续观察范围，不重写本窗口。R2-C有状态研究仍是条件项，R0正式合并与发布仍待维护者授权。
