# 72 小时只读观察已完成调度切换

后续首轮观察已实际发生：旧 AppData 绝对计划路径在宿主机不可达，首次采集失败；修复宿主路径后手动重试成功，两份结果均迟到，独立重放通过。巡检保留首次期限失败并识别恢复，通知因系统用户状态延后。详见[首轮故障与恢复](first-cycle-recovery.md)。下文及 activation.json 保留初始激活快照，不能用当时预演成功覆盖随后失败。

2026-09-13 收到维护者对 T5 切换的明确授权后，已通过 Codex 应用暂停旧 heartbeat，并于 **03:34:54 UTC（北京时间 11:34:54）**注册、启用两项 Windows 任务。当时状态为 **ACTIVATED_AWAITING_NATURAL_WINDOW**；初始核验尚未到达第一轮自然触发，72 小时验收未完成。

声明窗口为北京时间 **2026-09-13 12:00 至 2026-09-16 12:00（结束不含）**，巡检收尾至 12:10。新运行件来源提交 `bbf7cd2c452d6d059588459f84a2de5d818cf611`，清单摘要 `27cfe11a7a0c7246814e7f6966f5bed7a41b65f11a64e8ddf8a688847fdc3732`。六个脚本与此前已验收候选的精确字节一致；原 09:00 开始候选已过准备期限，保留原目录和摘要，未修改其窗口或重新封装旧回执。

| Windows 任务 | 首次自然触发（北京时间） | 周期 | OS 执行上限 | 停止边界（北京时间） |
|---|---|---|---|---|
| HakimiReadOnlyObservation | 9 月 13 日 12:01:00 | 每小时 | 10 分钟 | 9 月 16 日 12:00 |
| HakimiObservationWatch | 9 月 13 日 12:00:30 | 每分钟 | 50 秒 | 9 月 16 日 12:10 |

实际完成的验证见 [activation.json](activation.json)：

- 旧 heartbeat 从 ACTIVE 变为 PAUSED，回读确认仅状态和更新时间变化；原名称、提示词、周期及目标任务保留。切换前未发现原冻结观察包装器或驱动仍在运行。
- 原部署源码、环境和运行脚本预检通过，激活后身份与创建清单时一致。固定六个工具、巡检计划和清单绑定均通过核验。
- 创建时两项任务均禁用；保存注册回执后启用。实际任务回读核对固定命令、清单摘要、时间边界、周期和期限，以及 InteractiveToken、LeastPrivilege、IgnoreNew。
- **03:35:18 UTC 实际通过 Start-ScheduledTask 调用这两项已注册任务**。系统启动固定 `pythonw.exe`，两个任务均正常结束，LastTaskResult 为 0；同时验证实际保存的启动、结束回执及绑定关系。
- 巡检保存有效报告：72 个 PLANNED、0 个变化、0 次通知投递；观察入口在窗口前返回 OUTSIDE_DECLARED_RUN_WINDOW，未创建行情观察尝试。此项只证明 OS 入口和窗口限制，不能替代自然触发或连续观察。

本地证据脚本有两次检查失败：Windows 导出 XML 省略默认 RunLevel，随后也发现省略默认 Enabled。改用实际已注册任务的 COM 属性核对，分别为 RunLevel=0、LogonType=3、Enabled=true，验证通过。没有为修正证据脚本而更改任务或运行件，首次失败在 JSON 中保留。

运行要求保持当前用户登录、电脑和网络可用。窗口结束后停止新的观察调用，巡检多运行十分钟处理最终状态与待发通知；结束仅产生等待验收状态，旧 heartbeat 不会自动恢复。原 72 小时历史的 106 准时、14 迟到、24 缺失、0 已记录失败保持原口径，不能用新窗口补写。

本次授权及激活范围为原 BTC 研究程序和独立巡检。没有启用股票行情采集、官方券商模拟接口或真实下单，也没有合并 PR 或发布新 Release。PR #6 继续保留草稿，完整新窗口需要实际经过并核验全部回执。

公开 JSON 是指定字段投影，保留原回执摘要；含本机路径、用户 SID、任务 XML 和旧 heartbeat 提示词的原件仅保存在本机。清单中的 `scheduler_activated=false` 是创建时不可变快照；后续状态由独立注册回执和实际任务回读证明，不修改旧清单来反映当前状态。
