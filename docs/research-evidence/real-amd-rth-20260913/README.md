# 真实 AMD 首个研究闭环与富途只读核查

2026-09-13。此记录补充此前的[冻结来源与方案](../real-equity-source-20260913/README.md)，不改写旧记录的当时状态。

固定 AMD、2024-10-01 至 2024-11-20，共37个交易日。通过维护者已登录的本机 Futu OpenD 读取真实数据，未下单。保留完整原件、请求时间、参数及失败记录于本机；公开文件仅包含规格、摘要和内容身份，不含账户编号、持仓明细或行情序列。

## 数据接纳

原始 `K_DAY` 显式不复权，返回37行；另取 `K_60M / Session.RTH / AuType.NONE`，返回259行，无下一页。37天每天七条常规时段记录完整，聚合 OHLC 与原日线逐日相同，成交量却37天均有差异。差异原因未确认，不能把日线成交量直接解释成常规时段成交量。

实际快照使用这259条真实常规时段记录聚合：首开、最高、最低、末收、成交股数求和；没有插值、补造或改窗口。来源标为 **由真实分时聚合的日线**，没有冒充未加工的供应商日线导出。原日线与分时原件均保留。快照为 `38ce95abc3d7c1a5ac47be67f13b01881281cbbe0d30a06037dad452e09baffb`。

独立 Nasdaq 2024 交易日历逐日检查51个自然日，与富途返回的37个交易日一致，纽约夏令时转换已核对。公司行为接纳结合实际 `get_rehab`、AMD 官方拆股历史、2024年年报股东权益及股份变动表、同期8-K和身份资料，声明这个有限窗口没有需要调整研究账户持有股数或现金的行为。不是仅凭当前FAQ或空列表放行；该声明不认证完整市场事件、盘中停牌、开盘可成交性，也不代表人工财务字段核准。年报只用于事后数据接纳，不进入历史策略决策。

API访问成功与维护者只读授权支持本机研究范围；未确认再分发许可，因此原件和完整轨迹留在本机。历史日线可用时间仍是收盘后60秒假设，日程60+60秒亦为假设，实际采集发生于2026年。

## 冻结结果

| 项目 | 买入持有工程基线 | A：5/10均线 | B：财报日新增买入过滤 |
|---|---:|---:|---:|
| 总收益 | -3.4394% | -1.5974% | -1.5974% |
| 最大回撤 | 5.8261% | 1.5974% | 1.5974% |
| 成交笔数 | 1 | 4 | 4 |
| 完整交易 | 0 | 2 | 2 |
| 被拦截买入意图 | 不适用 | 0 | 0 |

买入持有沿用已冻结25%预算、成本和期末盯市，使用现有入口最早合法评分日10月2日；该规格补齐规则在计算结果前保存。A/B严格沿用原5/10均线、12个预热交易日、10月17日开始评分、25%仓位和原成本/退出参数，没有搜索参数。两组各25次价格决策完整保留，结果均亏损且相同。**本样本未识别到这条过滤规则的经济影响。**B不主动卖出财报前已有仓位。

三份报告分别通过独立 Decimal 账本核算（247、208、208项检查）及同版本离线重放；关闭事件规则的完整价格结果与原价格入口逐字节一致。实际计算使用组合提交 `8333a684…` 的普通 wheel，在仓库外独立环境运行，禁用网络补数。该安装通过183项根目录测试；组合仓库专用检查另为205项通过，不能合计成独立研究样本或远端CI。

结果、规格、来源身份和回执见 [research-summary.json](research-summary.json)。原始账本、全部信号和净值轨迹留在本机。C/D仍未运行，人工财务字段核准另行推进。

## 官方模拟与实际运行

[富途只读结果](futu-readonly.json)：已识别并明确使用真实返回的美股模拟账户ID，成功查询资金、持仓、订单；成交接口实际返回“模拟交易不支持成交数据”。官方[模拟交易说明](https://openapi.futunn.com/futu-api-doc/en/qa/trade.html)也说明不支持成交明细/推送、订单费用和现金流水查询。账户地区不能从模拟账户反推，价格步长、实际费用和结算仍未验收。多次查询不是原子账户快照；缺失费用不是零费用。现有执行状态机保持 `LOCAL_SIMULATOR`，官方适配器及订单生命周期尚未完成。

[运行进展](observation-progress.json)：首轮失败、人工恢复后的LATE均保留；随后05/06/07/08 UTC四个自然小时为ON_TIME，8份实际策略记录以原输入及固定安装独立重放通过。当前首次期限统计为1 FAILED、4 ON_TIME、67 PENDING；两条通知仍积压，没有本轮显示确认。当前运行件仍为 `bbf7cd2…`，候选 `d4afcca…` 没有原位替换。窗口仍于北京时间9月16日12:00结束，巡检延至12:10，不重启窗口或恢复旧heartbeat。

本分支把四个PR的指定版本组合在一起，保留各原始提交；这是可审查的集成候选，没有代替维护者执行主分支合并或发布。
