# 完整离线流程性能测量

使用CI33969915599下载的0.2.1 Windows wheel，运行时源码48f1c488…；本机Python3.14.6。每项命令在新进程串行执行，先前研究和重放进程均已结束；普通系统和桌面负载未隔离。没有应用性能优化或关闭保护检查。

| 样本 | 输入/评分根数 | 原始导入 | 快照读入与校验 | 研究执行 | 序列化 | 校验并保存 | 重放 | 完整流程 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 真实2025Q4，DualMA基准成本 | 2280/2208 | 0.325s | 0.348s | 2.217s | 0.013s | 0.090s | 3.639s | 7.631s |
| 合成5000 | 5000/4928 | 0.647s | 0.746s | 9.108s | 0.036s | 0.187s | 9.459s | 20.954s |
| 合成20000 | 20000/19928 | 2.894s | 3.086s | 23.246s | 0.086s | 0.481s | 22.343s | 50.795s |

原始导入另在新进程测量，不包括生成或联网采集。完整流程列从模块导入开始，包含快照/规约读取、唯一Runner执行、报告序列化与校验保存、再次读取、规范重放和回执保存；不把另一次原始导入命令的冷启动重复相加。全部阶段细项见JSON。

合成输入保留原性能样例的NumPy公式，但明确为SYNTHETIC_TEST；固定72根上下文，其市场收益没有解释意义。原旧记录仅测旧模型核心、上下文等口径不同，不能把本表与旧2.58秒/27.06秒直接解释成提速或回归。

## 一次单独插桩的热点观察

另做5000根cProfile，研究执行10.128秒；这个插桩结果不能与正常吞吐混为一组基准。快照校验在Runner中调用2次，累计约3.555秒；策略信号生成累计约2.603秒。嵌套累计时间重叠，不能相加当作总耗时。

这些数据足以把下一次受控优化实验聚焦于重复解析/校验与历史窗口/指标计算，但目前不据单次、未隔离桌面负载的样本宣布优化收益。若后续引入已验证快照复用，必须单独验证不可变输入、来源和每笔决策，不能通过关闭保护或改变阈值获得速度。

本轮4个完整流程（含插桩）全部精确重放，源码和依赖环境匹配；3次原始导入均重建出原快照身份。不同样本及同尺寸重放耗时存在波动，报告保留实际数值，未挑选最快一次。

[全部测量与身份核验索引](verification.json)。完整报告和含机器信息的重放原件保留本机；公开的是原始测量回执和逐项核对索引。
