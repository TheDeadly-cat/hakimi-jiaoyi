# 固定运行件与 Windows 预演

此页保留激活前的历史预演记录。后续收到授权，原候选窗口已过准备期限，因此另建未来窗口并完成[实际调度切换](../observation-activation-20260913/README.md)；下文的 NOT_ACTIVATED 和旧 heartbeat ACTIVE 均为当次预演状态。

北京时间 2026-09-13 已完成真实本机准备预演，仍为 **NOT_ACTIVATED**。来源为 `8ce5148ebc5958e3430d54df3c20d8dfd061e22d`，实际固定清单摘要为 `f1940935939b2affa7679b5b63a226333545195ce47eda312df7ccf16b10be48`。六个脚本的精确本机字节身份和脱敏结果见 [preparation.json](preparation.json)。后续文档提交不会重写这个运行件的来源。

准备窗口为北京时间 **9 月 13 日 09:00 至 9 月 16 日 09:00（结束不含）**，巡检收尾至 09:10。这是待批准的有限运行方案，尚未开始；批准过晚则另建未来窗口，原件保留。窗口结束后不再触发观察，额外十分钟后停止巡检，旧 heartbeat 不会自动恢复。

实际完成的检查：

- 使用原部署安装解释器核验冻结源码、依赖及三个运行脚本，准备前后身份一致。
- 通过 Windows Schedule.Service 创建两份内存任务定义，检查实际 XML：UTC 边界、小时/分钟周期、10 分钟/50 秒期限、InteractiveToken、LeastPrivilege、IgnoreNew、初始禁用及固定命令均符合预览。没有调用注册或启用。
- 以准备任务的实际 `pythonw.exe` 命令运行巡检入口，得到有效保存的报告；72 个时点全部 PLANNED，没有事件或通知队列。观察入口在窗口开始前明确跳过，未创建行情观察回执。
- 两次无窗口入口均退出 0，所属作业清理确认；核验实际产物，不仅看退出码。旧 heartbeat 配置字节未变且仍 ACTIVE，目标 OS 任务查得 0 个。
- 修复后 175 项仓库配套测试通过，包含 10 项运行件检查；新增日期回归实际调用 Windows PowerShell 与 PowerShell 7。未把不同轮次或子集相加。

首轮预演确实失败：提交 `03b0b33` 的 PowerShell JSON 解析将时间变为 DateTime，向 COM 赋值时变成 `2026/9/13 1:00:30` 等无时区字符串。检查在注册前拒绝；修复显式转为 UTC ISO 字符串后另建运行件，没有修改旧候选文件。中间的验证脚本还曾在执行入口前因 AppData 重定向别名与规范路径比较失败；只修正验证脚本的路径解析，第二个运行件字节未变。两项首次失败均保留说明和原始本机材料。

本次没有新的行情采集、原生通知显示尝试、OS 注册/启用、官方券商调用或新 72 小时验收。此前 [完整链路故障恢复](../observation-full-chain-20260913/README.md) 和 [独立巡检通知显示](../observation-watch-20260913/README.md) 保持原代码与回执身份；本次预演不把它们改记为当前运行件的自然观察结果。

下一步仅在维护者批准后执行 [受控切换](../../observation-control-bundle.md)。实际注册、启用失败时的回退以及连续 72 小时结果仍需对应真实运行证据。
