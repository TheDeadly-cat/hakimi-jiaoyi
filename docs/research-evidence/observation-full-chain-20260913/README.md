# T5：冻结程序隔离故障演练与退出竞态修复

本轮实际调用了复制到仓库外的冻结 Python、包装器、驱动和采集器。安装源码与依赖验证均通过；复制的 3,710 个文件中，仅新副本的 `forward/deployment-plans.json` 改为指向副本内的计划路径。原计划、工具、安装环境及原部署受保护文件逐项核对字节未变。旧周期数据没有复制，因此副本里的历史缺失不能算作生产缺口。

最新结果：两个隔离副本已在随后真实小时恢复有效观察并完成重复校验，见[次小时恢复原件投影](next-hour-recovery.json)。T5仍缺独立通知巡检、用户看到确认、授权切换和新72小时观察。

后续增量：[Windows原生显示与投递去重](../observation-notification-20260913/README.md)已有一次系统显示回执；用户看到确认、独立巡检和调度切换尚未完成。本目录的NOT_RUN字段保持各次证据快照原状。

[fault-drill.json](fault-drill.json)保存当前公开投影、原回执摘要和验证范围。原始失败记录、机器路径、完整日志和临时安装副本保留在本地，未加入仓库。旧启动器验收字节与这次修复后的启动器摘要分别记录。

## 实际结果

| 演练 | 观察到的结果 |
|---|---|
| 本地代理拒绝网络 | 真正采集器发出一次 CONNECT，独占 loopback 代理返回 503，从未转发外部请求；没有形成输入快照或观察记录 |
| 同一实际小时重试 | 不完整小时目录仍保留，原驱动拒绝重取并混合数据；启动器和冻结包装器的锁均可重新取得 |
| 持有采集连接时重复调用 | 第二个启动器返回 `DUPLICATE_IN_FLIGHT`，没有产生第二次采集连接或有效结果声明 |
| 强制终止自建启动器 | 仅使用该启动器原有 Popen 句柄；终止前观察到的 10 个后代进程句柄在约 0.046 秒内全部转为退出状态 |
| 终止后重试 | 保留启动而未结束的回执，重试识别该未完成任务并释放锁；同一不完整小时仍拒绝，未改写为成功 |

所有调用均为隔离副本的实际 `MANUAL` 调用；没有伪造整点、调度事件或信号时间。强制终止演练时的进程祖先快照仅用于保留只读等待句柄，未用发现的 PID 或端口终止其他进程。

## 首轮暴露的竞态

网络拒绝首轮没有通过预期分类断言：预期 `CHILD_FAILED`，实际为 `CHILD_LEFTOVER_DESCENDANTS`，但清理已确认且结果无效。该失败原件保留。

随后对同一已失败小时进行八次有限、无重取的诊断调用，复现一次：作业首次计数为 1；取得成员句柄时该句柄已退出，零等待返回 0；再查作业已为 0。旧实现仍依据首次计数报告残留。这里只能确认退出竞态，未能取得已退出进程的镜像名称，不推断具体进程类型。另三次采集器单项拒绝探针没有复现，单项结果不替代完整链路证据。

修复在取得成员句柄后，立即复核句柄已退出且作业已空，满足两项时保留正常退出分类；仍有活跃成员时继续失败、终止本次作业并等待清理。没有增加宽限 sleep、改变超时和输出上限，或放松实际残留后代的拒绝条件。

针对这一竞态的回归在修复前失败，修复后包含它的 25 项启动器测试通过；按 CI 的仓库导入路径执行的 133 项配套测试通过，包含前述 25 项。首次回归夹具有 Windows 换行预期错误；首次配套测试调用漏设仓库导入路径，85 项加载/测试记录中有 9 项导入错误。两类调用错误与有效的修复前失败均保留，不计为成功证据。更新后的实际冻结链路同小时重试确认 `CHILD_FAILED`、进程边界 `EXITED`、清理完成；该小时仍没有有效观察。

## 远端 CI 暴露的控制台收尾

初次竞态修复提交 `4f9ac1db8dfdf1783d678c1523f2746c28c75a29` 的 [CI 34715489973](https://github.com/TheDeadly-cat/hakimi-jiaoyi/actions/runs/34715489973) 未通过：133 项配套测试中，正常退出测试仍被判为残留。该失败没有重跑后覆盖。对相同正常退出命令的追加诊断首轮即复现，保留的等待句柄对应 `conhost.exe`，零等待返回 258（当时未退出）；稍后作业已空。仅增加即时复核不足以覆盖 Windows 控制台的正常收尾。

后续修复在原 5 秒清理预算内，给正常退出的成员最多 250 毫秒等待时间；若清理预算更小，最多使用其中一半。通过真实进程句柄等待退出，再确认作业已空；到期仍未退出就继续报告残留并清理本次作业。超时和输出超限不会取得此正常退出等待，整体执行期限与合并输出上限不变。回执新增实际适用的 `normal_exit_settle_max_seconds`。26 项启动器检查与包含它们的 134 项配套检查通过，包括短暂收尾正常结束，以及真正持续残留的后代仍被拒绝和清理；原 25/133 项结果保留为前一个候选的证据。

## 随后真实小时恢复

事先声明手动调用区间为 `2026-09-12T20:01:00Z` 至 `20:05:00Z`，没有修改任何程序时钟或注册调度。实际执行时间分别为 `20:01:21Z` 和 `20:01:29Z`：断网副本及终止副本各读取一页 OKX 公开历史小时数据，覆盖原冻结驱动所需的72小时输入上下文。两者均对新的 `20:00Z` cutoff 形成两条观察并通过原包装器的两条重放校验，启动器返回 `RESULT_VALID`，记录恢复通知决定。这个72小时输入范围不是72小时持续运行验收。

随后各重复调用一次，返回 `DUPLICATE_VERIFY` / `DONT_NOTIFY`，当前周期全部文件摘要保持一致；旧失败小时与启动未结束的回执全部保留。两个副本是同一工程恢复验证的两个故障案例，不作为四个独立市场样本或策略收益证据。`next-hour-recovery.json`保存两次独立下载的实际时钟和各自摘要；没有把同一输入重新签名为另一份下载。

恢复使用外层启动器的新字节 `9ed6bbeba4b9b81c55edb6e3ff6e03aa4d95cdd9a34ac6cc934ca5213e1ed006`，原冻结观察程序源码、环境和计划身份仍各自核验。早期 `fault-drill.json` 中的 NOT_RUN 保留为那个证据快照的状态，后续结果独立保存。

## 后续门槛

后续独立投递部件已有一次Windows显示回执，见上方链接；观察启动器自身仍不发送通知。独立巡检、用户看到确认、单一调度权切换和新的72小时观察尚未完成。本轮没有暂停原heartbeat、注册OS任务或改变研究包交易权限。后续激活须经维护者明确批准并固定实际运行件，不能把本轮隔离副本的恢复当作原部署已经完成切换。
