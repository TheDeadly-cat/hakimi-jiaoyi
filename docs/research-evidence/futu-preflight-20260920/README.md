# 当前候选的只读预检

后续：维护者授权继续解决后，已修复零价格记录令只读诊断提前退出的问题，并完成两轮真实账户读取与证券核验。未知记录仍阻挡执行，详见[授权后的排查与修复](resolution-followup.md)。下文保留最初失败与协议诊断的原始时间及结论。

2026-09-20，北京时间05:00完成一次新的有界读取。OpenD已由维护者打开，SDK固定为10.7.6708，子进程只连接本机OpenD；Windows Job上限180秒、输出128KiB、清理5秒。

`get_acc_list`、`order_list_query`、`history_order_list_query`均返回RET_OK，指定模拟账户身份检查已通过。后续订单投影遇到一条非本提案的零价格、未知状态记录，按原校验停止，结果为`PREFLIGHT_INCOMPLETE / provider_order_quantity_or_price_invalid`。没有完成两轮账户一致性读取、证券规则检查或新数据库准备。

这条记录不能被猜成已结束、无需处理或没有风险。本轮未改历史查询起点、过滤既有订单或放宽价格/状态检查，未撤销它，也未发送本提案订单。账户号、订单原件、持仓与日志只留本机；[公开回执](preflight.json)只记录方法、时间、返回码、来源摘要与进程边界。

## 原始协议诊断

维护者随后报告官方模拟界面没有显示、未查到该记录。北京时间05:39另做一次180秒/128KiB/5秒约束下的只读诊断，实际业务协议查询严格限制为账户列表、刷新当前订单、原起点历史订单各一次。当前与历史响应的真实protobuf header均明确包含并匹配指定账户、SIMULATE和US；两次返回同一条非本提案记录。

原始 `orderStatus` 字段存在，数值为 **-1**，正是固定SDK已知的 `OrderStatus_Unknown`，解码成`N/A`是正确行为。原始价格字段存在且为零。因此本次证据排除了“SDK不认识新枚举”的解释，没有证明取消、失效或任何终态。

界面可见性仅为维护者报告，本轮未独立核对其界面所选账户与筛选条件；与API的差异继续保留。没有因界面查不到就忽略记录，也未换SDK、删记录、换账户、撤单或改数据库。需要提供方说明或新的可核验状态后，才能决定当前账户前置条件是否满足。详见[脱敏协议结果](protobuf-diagnostic.json)。原始protobuf及解码记录只存本机。

本次诊断在子进程中禁用SDK自动账户订阅/解锁，后续两次查询只复用刚取得且已验证的账户列表，以免SDK内部附加账户查询超出三次预算；SDK文件和profile字节未改动。三次均RET_OK，进程正常退出并确认清理；下单、撤单、数据库及授权操作均为零。最初预检的三条记录是显式方法调用摘要，不声称覆盖SDK内部握手、账户读取或订阅次数。

另一次本机只读检查确认9月13日旧数据库的落盘状态仍为PREPARED、无持久提交领取、无绑定订单，读取前后SHA-256一致；这不代替当前账户查询。新候选源码与旧冻结源码不同，没有重签或修改旧数据库。

后续实际周期仍需：核实并满足当前账户前置条件、从最终固定源码建立新的准备身份、维护者单独有效授权，以及实际常规交易时段的现场核对。原提案保持US.AMD BUY 1股、1美元限价、DAY/RTH，最多一次提交和一次对绑定订单的撤单；未知不重发、拒单不改价、意外成交不扩展卖出权限。当前没有操作授权文件。
