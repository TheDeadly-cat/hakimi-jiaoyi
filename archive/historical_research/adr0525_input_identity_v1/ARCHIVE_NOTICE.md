# ADR0525 Archived Input-Identity Fixture

These five files are the byte-identical former
`outputs/python_quant_bot/examples/deterministic_experiment` fixture. It checked
only 40-row input identity and never ran a strategy backtest.

It is archived, non-current, and grants no performance, ranking, paper, live,
or order authority. The active synthetic reference is
`examples/deterministic_frozen_benchmark_v1`.
